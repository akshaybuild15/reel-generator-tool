# See spec §12. Values live in GCP Secret Manager, never committed here.
# Local dev: populate this file from Secret Manager values, or rely on
# gcloud application-default credentials where the client library supports it.

GEMINI_API_KEY=
GCP_PROJECT_ID=
GCS_BUCKET_NAME=
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
CLOUD_TASKS_QUEUE=
